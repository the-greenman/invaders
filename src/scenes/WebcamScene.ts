import Phaser from 'phaser';

export class WebcamScene extends Phaser.Scene {
  constructor() {
    super({ key: 'WebcamScene' });
  }

  create(): void {
    // Placeholder - will implement later
    console.log('WebcamScene created');
  }
}
